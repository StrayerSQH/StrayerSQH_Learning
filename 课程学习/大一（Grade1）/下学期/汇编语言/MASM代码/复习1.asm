DATA SEGMENT
	STRING DB 'HELLOWORLD!'
	LEN EQU $-STRING
	POSITION DB ?
DATA ENDS

STACK1 SEGMENT STACK
	DB 40 DUP(0)
STACK1 ENDS

CODE SEGMENT
ASSUME CS:CODE, DS:DATA, SS:STACK1
BEGIN:
	MOV AX, DATA
	MOV DS, AX
	MOV ES, AX
	
	CALL SEARCH

SEARCH:
	PUSH AX
	PUSH DX
	XOR CX, CX
	MOV CL, LEN
	MOV SI, STRING
	LOOP LOP1£º
		CMP [SI], 'E'

	MOV AH, 4CH
	INT 21H
CODE ENDS
END BEGIN
