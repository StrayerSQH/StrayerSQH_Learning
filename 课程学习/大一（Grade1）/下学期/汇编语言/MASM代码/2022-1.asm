DATA SEGMENT
	STRING1 DW 40 DUP(0)
	MAXLEN DW 20
	STRING2 DW 40 DUP(0)
	REAL_LEN DW ?
DATA ENDS

STACK SEGMENT
	DW 40 DUP(0)
STACK ENDS

CODE SEGMENT
ASSUME CS:CODE, DS:DATA, SS:STACK
BEGIN:
	MOV AX, DATA
	MOV DS, AX
	
	LEA DX, MAXLEN
	MOV AH, 0AH
	INT 21H
	
	XOR CX, CX
	MOV CL, REAL_LEN
	LEA SI, STRING1
	LEA DI, STRING2
	


CODE ENDS
END BEGIN

