DATAS SEGMENT
    ;此处输入数据段代码  
DATAS ENDS

STACKS SEGMENT
    ;此处输入堆栈段代码
STACKS ENDS

CODES SEGMENT
    ASSUME CS:CODES,DS:DATAS,SS:STACKS
START:
    MOV AX,DATAS
    MOV DS,AX
    MOV    AL, 01H
	MOV    SI, 0002H
	LEA      SI, [SI]
	MOV     BYTE PTR [SI], 80H
	LAHF
	XCHG  AL, AH
	SAHF
	XCHG  AH, [SI]
	SAHF

    MOV AH,4CH
    INT 21H
CODES ENDS
    END START
