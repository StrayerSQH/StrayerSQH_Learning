DATA SEGMENT
	STRING1 DB 40 DUP(0)
	STRING2 DB 40 DUP(0)
	LEN1 DB ?
	LEN2 DB ?
DATA ENDS

STACK1 SEGMENT STACK
	DB 40 DUP(0)
STACK1 ENDS

CODE SEGMENT
ASSUME CS:CODE, DS:DATA, SS:STACK1
BEGIN:
	MOV AX, DATA
	MOV DS, AX
	MOV ES, AX
	
	MOV AH, 0AH
	INT 21H
	LEN1 EQU $-STRING1
	
	MOV AH, 0AH
	INT 21H
	LEN EQU $-STRING2

CODE ENDS
END BEGIN
