DATAS SEGMENT
    VAR1 DB 35H
    VAR2 DW 7455H
    	 DW 0E259H
    RES1 DB ?
    RES2 DB ?
DATAS ENDS

STACKS SEGMENT
STACKS ENDS

CODES SEGMENT
    ASSUME CS:CODES,DS:DATAS,SS:STACKS
START:
    MOV AX,DATAS
    MOV DS,AX
    
    MOV AL, VAR1
    MOV RES2, AL
    AND RES2, 0FH
    
    MOV RES1, AL
    AND RES1, 0F0H
    MOV RES1, AL
    
    MOV CL, 4
    SHR RES1, CL
    
    SHL VAR2, 1
    RCL VAR2+2, 1
    SHL VAR2, 1
    RCL VAR2+2, 1
    
    
    MOV AH,4CH
    INT 21H
CODES ENDS
    END START
