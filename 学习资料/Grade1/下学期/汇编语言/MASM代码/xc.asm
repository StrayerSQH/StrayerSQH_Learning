DATAS SEGMENT
    ;此处输入数据段代码  
DATAS ENDS

STACKS SEGMENT
    ;此处输入堆栈段代码
STACKS ENDS

CODES SEGMENT
    ASSUME CS:CODES,DS:DATAS,SS:STACKS
START:
    MOV AX,DATAS
    MOV DS,AX
    SUB AX, 28
MOV BX, AX
MOV CX, 10
LOOP S

S:
ADD AX, BX
DEC CX
LOOP S

ADD AX, 5
MOV CX, 3
SHR AX, CX

    MOV AH,4CH
    INT 21H
CODES ENDS
    END START
