DATA SEGMENT
	MAXLEN DB 30
	INPTLEN DB ?
	STRL DB 31 DUP(0)
DATA ENDS

STACK SEGMENT
	DW 40 DUP(0)
STACK ENDS

CODE SEGMENT
ASSUME CS:CODE, DS:DATA, SS:STACK
BEGIN:
	MOV AX, DATA
	MOV DS, AX
	
	MOV SI, OFFSET STRL
	MOV CL, 30
READ_LOOP:
	MOV AH, 01H
	INT 21H
	MOV [SI], AL
	CMP AL, 0DH
	JE EXIT
	INC SI
	LOOP READ_LOOP
	
	MOV AH, 09H
	MOV DX, PFFSET STRL
	INT 21H
	
	MOV AH, 4CH
	INT 21H
CODE ENDS
END BEGIN
